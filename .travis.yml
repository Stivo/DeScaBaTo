# See http://about.travis-ci.org/docs/user/build-configuration/
# triggers docker
sudo: false
language: scala
scala:
  - 2.12.6
branches:
  only:
    - master
    - feature/no_rocks
jdk:
  - oraclejdk8
#before_script:
#   - sudo apt-get install par2
